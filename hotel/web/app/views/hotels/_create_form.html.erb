<%= form_tag(:controller => "hotels", :action => "create", method: "post") do %>
  <p>
    <%= label_tag(:name, "Name") %>
    <%= text_field(nil, :name, size: 75, :required => true) %>
  </p>
  <p>
    <%= label_tag(:address, "Address") %>
    <%= text_field(nil, :address, size: 75, :required => true) %>
  </p>
  <p>
    <%= label_tag(:accommodation_type, "Accommodation Type") %>
    <%= select(
      nil,
      'accommodations_id',
      accommodations['accommodations'].each.map {
        |accommodation| [accommodation['type'], accommodation['id']] 
      },
      :selected => 0) %>
  </p>
  <p>
    <%= label_tag(:stars_rating, "Stars") %>
    <%= select(
      nil,
      'star_rating_id',
      stars['stars_rating'].each.map {
        |star| [star['rating'], star['id']] 
      },
      :selected => 0) %>
  </p>
  <%= submit_tag("Create") %>
<% end %>
